{
  "name": "jump-to-line",
  "main": "./lib/index.js",
  "version": "0.0.0",
  "description": "Rapidly jump to the start of any line that is currently visible",
  "activationCommands": {
    "atom-workspace": [
      "jump-to-line:start"
    ]
  },
  "keywords": [],
  "repository": "https://github.com/jmorrell/atom-jump-to-line",
  "license": "MIT",
  "engines": {
    "atom": ">=0.174.0 <2.0.0"
  },
  "dependencies": {
    "underscore-plus": "^1.6.6"
  },
  "scripts": {
    "flow": "flow; test $? -eq 0 -o $? -eq 2"
  },
  "devDependencies": {
    "flow-bin": "^0.35.0",
    "nuclide": "^0.183.0"
  }
}
